// window.BattleAnimations = {
//     async attack(event, onComplete) {
//         const animationName = event.caster.team === "player" ? "hero_walk-right" : "slime_death";

//         element.addEventListener("animationend", () => {

//         }, { once: true });
//     }
// }